import { EmptyState } from "../components/EmptyState.jsx";

export function CourseBuilderPage() {
  return (
    <EmptyState
      title="Course Builder"
      message="Ordered modules, video URLs, reading materials, and end-of-module quizzes."
    />
  );
}
