import { EmptyState } from "../components/EmptyState.jsx";

export function AdminDashboard() {
  return (
    <EmptyState
      title="Admin Dashboard"
      message="Manage users, roles, platform health, and course publishing oversight."
    />
  );
}
